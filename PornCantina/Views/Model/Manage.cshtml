@model IEnumerable<PornCantina.Models.Model>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                Active Galleries
            </th>
            <th>
                Inactive Galleries
            </th>
			<th>
				Webmaster Content Link
			</th>
			<th></th>
        </tr>
    </thead>
    
@foreach (var item in Model) {
    <tbody>
        <tr>
            <td>
				<a href="@Url.Action("Details", "Model", new { Id = item.Id })">@Html.DisplayFor(modelItem => item.Name)</a>
            </td>
            <td>
                @item.GetActiveGalleryCountByModel(item.Id)
            </td>
            <td>
                @item.GetInactiveGalleryCountByModel(item.Id)
            </td>
            <td>
                <a href="@item.WebSite.WebmasterContentPage">@Html.DisplayFor(modelItem => item.WebSite.WebmasterContentPage)</a>
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id })
            </td>
        </tr>
    </tbody>
}

</table>

<div>
    @Html.ActionLink("Back to Admin", "../Admin")
</div>