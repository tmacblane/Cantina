@model IEnumerable<PornCantina.Models.WebSite>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <thead>
        <tr>
            <th>
                Model Site
            </th>
            <th>
                Referral Link
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a href="@item.Name">@Html.DisplayFor(modelItem => item.Name)</a>
            </td>
            <td>
                <a href="@item.ReferralLink">@Html.DisplayFor(modelItem => item.ReferralLink)</a>
            </td>
            <td>
                <a href="@item.WebmasterContentPage">@Html.DisplayFor(modelItem => item.WebmasterContentPage)</a>
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id })
            </td>
        </tr>
}
    </tbody>
</table>


<div>
    @Html.ActionLink("Back to Admin", "../Admin")
</div>
